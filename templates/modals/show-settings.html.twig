<div class="modal fade" id="show-settings" tabindex="-1" role="dialog" aria-labelledby="modal" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="modal-title"></h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <label for="offset">{{ 'show.offset (hours to add) 0 to use default'|trans }}</label>
                <input class="form-control show-offset" name="offset"
                       type="number" min="0" step="1" value="" placeholder="{{ 'show.offset'|trans }}"
                       title="{{ 'show.offset (hours to add) 0 to use default'|trans }}">
            </div>
            <div class="modal-footer">
            </div>
        </div>
    </div>
</div>
