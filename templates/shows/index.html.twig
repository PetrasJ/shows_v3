{% extends 'base.html.twig' %}

{% block body %}
    {% for show in shows %}
        {{ show.name }} <a href="{{ path('unwatched-episodes', {'showId': show.id}) }}">{{ show.episodes }}</a> <br>
    {% endfor %}
{% endblock %}
