{% extends 'base.html.twig' %}

{% block body %}
    {{ shows|length }}
    <div class="row">
        <div class="col-3">
            {{ 'show.show'|trans }}
        </div>
        <div class="col-3">
            {{ 'show.Previous Episode'|trans }}
        </div>
        <div class="col-3">
            {{ 'show.Next Episode'|trans }}
        </div>
        <div class="col-3">
            {{ 'show.Actions'|trans }}
        </div>
    </div>

    {% set showstatus = null %}
    {% for show in shows %}
        {% if (showstatus != show.status) %}
            {% set showstatus = show.status %}
            <div class="col-12">
                <h4>{{ ('show.'~show.status)|trans }}</h4>
            </div>
        {% endif %}
        <div id="{{ show.id }}" class="row py-1 border-bottom">
            <div class="col-3">
                <a href="{{ path('shows_details', {'showId': show.id}) }}">
                    {{ show.name }} ({{ show.episodesCount-show.watchedCount }})
                </a>
            </div>
            <div class="col-3">
                {% if show.lastEpisode %}
                    {{ show.lastEpisode.userAirstamp|dateTimezone }}
                {% endif %}
            </div>
            <div class="col-3">
                {% if show.nextEpisode %}
                    {{ show.nextEpisode.userAirstamp|dateTimezone }}
                {% endif %}
            </div>
            <div class="col-3">
                <button data-toggle="modal" data-target="#show-settings"
                        class="btn btn-warning show-settings"
                        data-id="{{ show.id }}"
                        data-title="{{ show.name }}"
                        data-offset="{{ show.offset }}">
                    <i class="fa fa-cog" aria-hidden="true"></i>
                </button>
            {% include 'shows/actions.html.twig' with {'show': show, 'status': status} %}
            </div>
        </div>
    {% endfor %}
    {% include 'modals/show-settings.html.twig' %}
{% endblock %}
