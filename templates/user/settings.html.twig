{% extends 'base.html.twig' %}

{% form_theme form 'form/settings.html.twig' %}

{% block body %}
    {{ form(form) }}
{{ duration }} {{ 'Days'|trans }}<br>
    {% for episode in lastEpisodes %}
        <u>{{ episode.created|dateTimezone }}</u> {% include '_inc/episode-details.html.twig' with {episode: episode} %}
        <b>{{ episode.showName }}</b> {{ episode.name }}<br>
    {% endfor %}
{% endblock %}